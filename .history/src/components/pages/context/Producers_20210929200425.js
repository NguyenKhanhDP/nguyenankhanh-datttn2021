import React, { createContext } from 'react'

const Cart = createContext();


const Producers = ({ chilren }) => {
    return (
        <Cart.Provider>
            
        </Cart.Provider>
    )
}

export default Producers
