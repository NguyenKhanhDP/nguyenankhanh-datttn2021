import React, { createContext } from 'react'

const Cart = createContext();


const Producers = ({ chi }) => {
    return (
        <Cart.Provider>
            
        </Cart.Provider>
    )
}

export default Producers
